server {
    listen 80;
    server_name _;

    location /health {
        proxy_pass http://health-api:8080/health;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
